#!/bin/sh -e

if [ whoami != "${USER_NAME" ]; then
    if [ -w /etc/passwd ]; then
        echo "${USER_BAME:-llleaas}:x:$(id -u):$(id -g):${USER_NAME:-llleaas} user:${HOME}:/sbin/nologin" >> /etc/passwd
    fi
fi

exec ${LLLEAAS} $@